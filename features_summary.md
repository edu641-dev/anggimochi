# 질환별 맞춤 생활 수칙 및 식단 가이드 - 주요 기능 명세서

이 문서는 본 프로젝트(`healthcare_guide.html`)에서 구현된 주요 기능과 기술적 특징들을 정리한 내용입니다.

## 1. 데이터 관리 및 구조 (Data Management)
*   **자체 데이터베이스 내장**: 별도의 백엔드 서버 없이, HTML 파일 내부에 JavaScript 배열(`diseases`) 형태로 약 100여 개의 질환 데이터를 하드코딩하여 관리합니다. (구글 스프레드시트 데이터 기반)
*   **구조화된 질환 정보**: 각 질환은 아래와 같은 세부 속성으로 체계적으로 관리됩니다.
    *   `name`: 질환명 (예: 고혈압)
    *   `category`: 분류 (예: 순환기 질환, 내분비 질환 등)
    *   `behaviors`: 하면 좋은 행동 / 생활 수칙 배열
    *   `goodFoods`: 먹으면 좋은 추천 식품 배열
    *   `badFoods`: 주의하거나 피해야 할 금기 식품 배열

## 2. 사용자 맞춤형 검색 및 필터링 (Search & Filter)
*   **동적 카테고리 생성**: `renderCategories()` 함수를 통해 데이터베이스에 존재하는 카테고리 종류를 자동으로 추출하여 상단에 필터 버튼으로 렌더링합니다.
*   **실시간 통합 검색**: `filterDiseases()` 함수를 사용하여, 사용자가 선택한 '카테고리'와 검색창에 입력한 '질환명'을 동시에 만족하는 결과만 실시간으로 걸러내어 화면에 표시합니다.

## 3. 직관적인 화면 전환 (SPA Navigation)
단일 페이지 애플리케이션(SPA) 방식으로, 페이지 새로고침 없이 부드러운 화면 전환을 제공합니다.
*   **목록 화면 (List View)**: 검색창, 카테고리 필터, 그리고 필터링된 질환들이 텍스트 위주의 '카드(Card) 형태'로 나열되는 메인 화면입니다.
*   **상세 화면 (Detail View)**: 질환 카드를 클릭하면 검색창과 목록이 숨겨지고(`display: none`), 해당 질환의 3가지 핵심 정보(생활 수칙, 추천 식품, 금기 식품)가 펼쳐지는 화면입니다.
*   **뒤로 가기**: `goBackToList()` 함수를 통해 상세 화면에서 다시 이전 검색 상태를 유지한 채 목록 화면으로 되돌아갈 수 있습니다.

## 4. 고령자 친화적 UI/UX (Accessibility for Elderly)
*   **가독성 중심 디자인**: 복잡한 이미지를 제거하고, 큼직한 텍스트와 여백을 활용한 카드 디자인을 적용하여 정보 전달에 집중했습니다.
*   **글자 크기 조절 기능**: 화면 우측 상단에 글자 크기를 키우거나 줄일 수 있는 `A+`, `A-` 버튼을 배치했습니다. `changeFontSize()` 함수가 CSS 변수(`--base-font-size`)를 조작하여 화면 전체의 텍스트 크기를 동적으로 스케일링합니다.
*   **반응형 레이아웃**: CSS Grid(`grid-template-columns`)와 Flexbox를 적극 사용하여, PC 화면뿐만 아니라 모바일이나 태블릿 환경에서도 카드들이 화면 크기에 맞게 자동으로 정렬됩니다.

## 5. 주요 기술 스택
*   **언어**: 순수 HTML5, CSS3 (바닐라 CSS), JavaScript (바닐라 JS)
*   **특징**: 외부 라이브러리(React, jQuery 등)나 프레임워크에 의존하지 않고, 브라우저의 기본 DOM 조작 API만을 사용하여 가볍고 빠르게 동작하도록 설계되었습니다.
